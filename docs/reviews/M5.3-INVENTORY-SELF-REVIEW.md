# M5.3 只读 inventory 与 External namespace 自审

基线 a1c0b7fe82f8faaabd19c58e1dbf51e760a7a87e，基线 CI success。本轮增加只读 metadata inventory，为后续物理 preview 提供一致输入，不创建 journal、root reservation 或 native ownership proof。

inventory 在事务中重验 configuration checkpoint、选定旧 binding、全部根不重叠、reservation 和 maintenance 互锁，完整读取 Current/History placement 对应的 ArchiveVersion identity、lifecycle、integrity 和 length。不经过当前 portable unit 列表筛选，所以 removed identity 的 retained Current/History 不会遗漏。真实输入离线不影响该纯数据库读取；返回内容不是可执行 manifest 或 readiness。

Begin 与 commit/恢复/compaction 的 namespace 检查收敛为公共 helper，纳入 active 或仍有恢复工作的 Plan 的 active External binding；其他 Plan 保存 External binding、激活和发布时也检查 reservation。只比较持久 canonical path/key，不扫描 External 输入。原有 Source/output 冲突与 inactive pending maintenance 保护保持。

测试新增 External 路径相等/包含冲突、inactive Plan 激活与保存互锁，以及 inventory 空选择、同根、缺失 History、配置过期、已有 journal 拒绝；成功测试确认无日志/保留记录写入，并覆盖从声明中移除的 Current 与双根 History 集合。最终 dotnet build 零 warning/error；529 项测试通过（Core 81、Application 90、Architecture 10、Archiving 50、Infrastructure 298），EF 无 pending model changes，git diff --check 通过。

容量未知的启动策略在 PRODUCT 中保持未决，未用未知值冒充空间足够。容量 API 调研参考 [Microsoft AvailableFreeSpace 文档](https://learn.microsoft.com/en-us/dotnet/api/system.io.driveinfo.availablefreespace?view=net-10.0)，其数值考虑当前用户配额；未把该 API 或 inventory 当作物理存储预留保证。等待维护者裁定未知容量是否允许显式覆盖后，再完成物理 preview 和启动门槛。
