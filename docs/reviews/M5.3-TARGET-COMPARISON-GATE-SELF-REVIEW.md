# M5.3 目标比较能力门槛自审

维护者确认：无法可靠识别目标大小写或 Unicode 比较规则时阻止迁移，不提供强制继续，不创建探测文件。正式产品、架构及维护协议已同步。

本次为事务目标检查引入独立只读比较端口。未装配适配器时返回稳定诊断 RELOCATION_TARGET_COMPARISON_UNAVAILABLE；适配器失败不会被降级为字面路径检查成功。成功后仍检查 namespace 并重读配置与 metadata，不能在检查期间漂移后返回成功。普通 inventory 检查仍不提供 Begin authority。

当前没有真实平台比较适配器，因此该入口默认阻断。测试替身仅验证编排，不证明平台文件系统比较语义。底层 Stage/恢复原语不是完整用户启动入口；后续 Begin 集成仍须完成并重验真实比较能力，不能以本次端口作为功能完成证据。

新增四种测试：未装配、能力未知、比较期间配置漂移、取消。验证拒绝返回成功、无 journal/reservation、旧 root binding 保持。已有 namespace 并发测试改为显式注入比较测试替身。

验证：dotnet build --no-restore 零警告/错误；dotnet test --no-build 报告 602 项通过（Infrastructure 361）；git diff --check 通过。既有 Windows 链接 fixture 的权限限制不变，本次不新增平台能力声明。
