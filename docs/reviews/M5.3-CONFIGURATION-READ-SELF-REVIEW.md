# M5.3 独立配置观察入口自审

基线：`92b8685f66eacbeb68f2c4750349093750b1a9fc`。本次只落实原始输入离线时的配置读取边界，不宣称迁移事务或 metadata switch 已完成。

入口复用 AuthoritativePlanWorkflow 的 strict document/identity 校验；每次读取，不缓存上次成功结果，不降级为备份 readiness。显式拒绝 inactive Plan，并在读取前后检查取消。依赖中没有 Scanner、Secret Store、Archiver 或 input binding resolver。

完整 PlanSemanticFingerprint 仅用于发现配置变化。它不是迁移相关语义指纹，不写回既有 ExecutionSemanticDigest，也不因显示名称变化便暗自裁定必须终止迁移；后续 journal 版本与 commit guard 必须明确比较边界。读取成功不表示 root safety、capacity、placement 或物理目标验证已成功。

集成测试使用真实 SQLite 与 strict document reader，覆盖 Managed/File-backed Secure + FILE_MANAGED 在无输入/Secret binding 下读取、文件丢失/损坏/PlanId 替换后拒绝回退、配置更新重读、inactive 与取消。尚未开放用户迁移入口，M5.3 保持 IMPLEMENTING。

本地验证：build 0 warning/error，全部 412 项测试通过，其中新增 6 项配置读取集成测试。上一提交 CI run `33742765546` 已通过，本轮 CI 单独验证。
