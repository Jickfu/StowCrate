# M5.3 完整显式恢复自审

基线 bdae5a7ac065584f0ef5e92f18979e15db02a934，基线 CI success。ResumeAsync 要求选定 PlanId/TransactionId，不创建新迁移；按持久阶段恢复条目、seal、commit 和 cleanup，不自动 compaction。启动仍不自动调用 pre-commit 恢复。

任何可恢复错误不在同一调用内重试；只读重查 journal，确认未提交才报告 ResumeRequired，已提交报告 CleanupPending；无法读取或 transaction 已改变返回 OutcomeUnknown，不猜测失败/成功。损坏和不属于 caller 的取消继续向上传播。提交成功后的清理直接使用已返回的 durable journal，避免额外可取消 Load 把永久成功反转为取消。

真实文件/SQLite 覆盖 Prepared、Staged、TargetsDurable 入口直到清理完成、错误 transaction、缺少清理适配器、未知 temp 拒绝。纯 Application 故障测试覆盖提交前/后响应丢失、读取不可用、读取损坏、提交后取消，确认不重放。所有真实 I/O 仅发生在测试 fixture，barrier 为注入测试能力。

dotnet build 零 warning/error，519 项测试通过（Core 81、Application 90、Architecture 10、Archiving 50、Infrastructure 288）。没有完成初始 preview/preflight 或 App/CLI 入口，M5.3 仍在实现中。
