# M5.3 启动清理恢复自审

基线：`7cf76893e546f615fde5caa552899b3210d23878`。本轮增加 Application 已提交迁移清理恢复、全量 validated journal 枚举与 ConfigDatabaseStartupCoordinator 接入，不新增 schema、不实现路径释放，不宣称完整 M5.3 或 App/CLI 用户入口已经完成。

启动枚举含 inactive Plan；完整 manifest/progress/device/reservation 校验失败即停止。workflow 重新从本次数据库 session 加载日志，再逐项使用 repository 的事务 API；枚举得到的旧快照不独立授权物理删除。Prepared/TargetsDurable 不复制、不发布、不提交，只报告 ResumeRequired。MetadataCommitted 无物理适配器时报告 CleanupPending；已记录 OldCopyAbsent 跳过重复删除，完成前仍通过 repository 重验。Completed 不再执行物理动作，明确保留 reservation，不把历史完成标记当作当前健康证明。

I/O、权限、并发和一般 repository 错误只留下 CleanupPending；corruption 向上传播，非 caller cancellation 不伪装成用户取消。caller cancellation 在已提交清理阶段只暂停剩余清理，成功 proof 的持久化沿用 repository 的不可取消边界；启动整体可以取消，但不回滚迁移。诊断只返回固定标识，不泄漏原始异常中的路径。

有 reservation 的 Plan 不进入旧 publish/retention recovery 或 History inventory，避免异常恢复组合绕过迁移互锁。异常状态的冲突日志仍保留，不自动修复、回滚或删除。迁移完成后的 journal/reservation compaction 与 pre-commit 显式恢复继续待办。

新增 18 项测试，覆盖三种 pre-commit 状态不变、已提交/删后日志落后恢复、inactive Plan、缺少适配器、旧对象漂移、损坏日志、重复完成后文件重新出现、publish recovery 冲突、无日志和错误/取消分类。真实文件与 SQLite 测试注入 barrier，不声称突然断电或真实跨卷保证。未操作真实用户备份。

验证：dotnet build 零 warning/error；487 项测试通过（Core 81、Application 85、Architecture 10、Archiving 50、Infrastructure 261）；EF 无 pending model changes，git diff --check 通过。基线 CI `33778049249` success。本轮跨平台验证以提交后的 CI 为准。
